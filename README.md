# Inventory-Management-System
Smart inventory management system

0)clone this repo inside the htdoc folder of XAMPP
1)create data base in phpmyadmin "inventory_system"
2) import sql file inside the DATABAS FILE folder in phpmyadmin
3)there is a  folder named Include, and inside it you can see config.php , adjest the variables relevent to your localhost configurations
